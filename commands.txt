# 1.New  -> File -> Enter
# web: gunicorn app:app => Paste this code in empty file of 'Procfile' file. (without any extension)

# 2.pip freeze > requirements.txt => This command generates a 'requirements.txt' file.

# 3.pip install -r requirements.txt => This command install all the libraries found in the requirements.txt file.

